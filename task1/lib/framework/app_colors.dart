import 'package:flutter/material.dart';

class AppColors {

    static const Color primary = Colors.red;
    static const Color secondary = Colors.redAccent;
    static const Color background = Color.fromARGB(255, 245, 245, 245);
    static const Color borders = Colors.grey;

    static const Color textTitles = Colors.white;
    static const Color textCommon = Colors.black87;
    static const Color textLabels = Colors.red;
    static const Color textHints = Colors.grey;
}
